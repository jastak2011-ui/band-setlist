# Optional server-side Spotify fallback for BPM / energy / key when Deezer has no tempo on file
# Uses Client Credentials only. No Spotify user login, redirect URI, scopes, or OAuth callback is needed.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

